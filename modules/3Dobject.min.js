// @ts-nocheck
import*as THREE from"three";const windowSize={height:window.innerHeight,width:window.innerWidth/2.5},scene=new THREE.Scene,camera=new THREE.PerspectiveCamera(75,windowSize.width/windowSize.height,.1,1e3),renderer=new THREE.WebGLRenderer;renderer.setSize(windowSize.width,windowSize.height),renderer.outputColorSpace=THREE.SRGBColorSpace,renderer.setAnimationLoop(animate),document.getElementById("3DChar").appendChild(renderer.domElement);const geometry=new THREE.BoxGeometry(1,1,1),material=new THREE.MeshBasicMaterial({color:65280}),cube=new THREE.Mesh(geometry,material);function animate(){cube.rotation.x+=.01,cube.rotation.y+=.01,renderer.render(scene,camera)}scene.add(cube),camera.position.z=5;